<p style="color: green"><%= notice %></p>

<h1>Reading lists</h1>

<div id="reading_lists">
  <% @reading_lists.each do |reading_list| %>
    <%= render reading_list %>
    <p>
      <%= link_to "Show this reading list", reading_list %>
    </p>
  <% end %>
</div>

<%= link_to "New reading list", new_reading_list_path %>
