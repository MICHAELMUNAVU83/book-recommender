<%= form_with(model: reading_list) do |form| %>
  <% if reading_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reading_list.errors.count, "error") %> prohibited this reading_list from being saved:</h2>

      <ul>
        <% reading_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_name, style: "display: block" %>
    <%= form.text_field :book_name %>
  </div>

  <div>
    <%= form.label :book_genre, style: "display: block" %>
    <%= form.text_field :book_genre %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
