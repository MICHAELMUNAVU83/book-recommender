
 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container px-lg-5">
           <% if user_signed_in? %> 
     <h5 class="navbar-brand text-light" > Hello <%= current_user.name %> </h5>
    <% else %>
      <h5 class="navbar-brand text-light"> Not a member ? <h5>
    <% end %>   
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 text-light">
              <% if user_signed_in? %>
        
             <li class="nav-item">
               <%= link_to "Home", root_path , class: "btn btn-dark mx-4" %>
            </li>
             <li class="nav-item">
               <%= link_to "Books for my genre", books_for_genre_path, class: "btn btn-dark mx-4" %>
            </li>
             <li class="nav-item">
               <%= link_to "My Reading List", reading_lists_path, class: "btn btn-dark mx-4" %>
            </li>
            <li class="nav-item">
               <%= link_to "All Reading Lists", extra_path , class: "btn btn-dark mx-4" %>
            </li>
              <li class="nav-item">
               <%= link_to "Sign Out", destroy_user_session_path, class: "btn btn-dark mx-4" %>
            </li>
        <% else %>
           <li class="nav-item">
              <%= link_to "Sign up" , new_user_registration_path , class: "btn btn-dark mx-4"%>
            </li>
            <li class="nav-item">
               <%= link_to "Sign in" , new_user_session_path , class: "btn btn-dark"%>
            </li>
        <% end %> 
            </ul>
          </div>
        </div>
      </nav>